import { component$ } from "@builder.io/qwik";

export interface ContactFormProps {}

export const ContactForm = component$<ContactFormProps>((props) => {
	return <div>ContactForm component works!</div>;
});
